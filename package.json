{
  "name": "portfolio-workspace",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "workspaces": [
    "portfolio"
  ],
  "scripts": {
    "dev": "cd portfolio && vite",
    "build": "cd portfolio && vite build",
    "preview": "cd portfolio && vite preview",
    "lint": "cd portfolio && eslint \"portfolio/**/*.{js,jsx}\""
  }
}
